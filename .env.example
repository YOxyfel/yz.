# Public site URL — used for sitemap, robots, and JSON-LD canonical URLs.
# Set the same value in Vercel → Project → Settings → Environment Variables.
NEXT_PUBLIC_SITE_URL=https://yanezhekov.dev

# Optional analytics (production only — scripts load when IDs are set)
# NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_HOTJAR_ID=1234567
